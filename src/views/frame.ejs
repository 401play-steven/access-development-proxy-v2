<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>401 Play Travel Platform</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .phone-container {
            width: 375px;
            height: 812px;
            background: #000;
            border-radius: 40px;
            padding: 10px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
            position: relative;
        }

        .phone-screen {
            width: 100%;
            height: 100%;
            background: #fff;
            border-radius: 30px;
            overflow: hidden;
            position: relative;
        }

        .phone-notch {
            position: absolute;
            top: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 150px;
            height: 30px;
            background: #000;
            border-radius: 0 0 20px 20px;
            z-index: 10;
        }

        .status-bar {
            height: 44px;
            background: #fff;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 20px;
            font-size: 14px;
            font-weight: 600;
            color: #000;
            border-bottom: 1px solid #e5e5e5;
            position: relative;
            z-index: 1;
        }

        .status-left {
            display: flex;
            align-items: center;
            gap: 4px;
        }

        .status-right {
            display: flex;
            align-items: center;
            gap: 4px;
        }

        .header-image {
            width: 100%;
            height: auto;
            display: block;
            object-fit: cover;
            background: #fff;
        }

        .webview-container {
            width: 100%;
            flex: 1;
            overflow: hidden;
            background: #fff;
            position: relative;
            min-height: 0;
        }

        .webview-iframe {
            width: 100%;
            height: 100%;
            border: none;
            display: block;
        }

        .footer-image {
            width: 100%;
            position: relative;
        }

        .footer-image img {
            width: 100%;
            height: auto;
            display: block;
            object-fit: cover;
            background: #fff;
        }

        .footer-overlay {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 100%;
            display: flex;
            pointer-events: none;
        }

        .footer-click-area {
            pointer-events: auto;
            cursor: pointer;
            flex: 1;
        }

        .footer-click-area.left-area {
            flex: 3;
        }

        .footer-click-area.right-area {
            flex: 1;
        }

        .content-wrapper {
            display: flex;
            flex-direction: column;
            height: calc(100% - 44px);
            width: 100%;
        }

        .webview-container.hidden {
            display: none;
        }

        .away-message {
            display: none;
            width: 100%;
            flex: 1;
            background: #fff;
            justify-content: center;
            align-items: center;
            font-size: 18px;
            color: #666;
            padding: 20px;
            text-align: center;
        }

        .away-message.visible {
            display: flex;
        }
    </style>
</head>
<body>
    <div class="phone-container">
        <div class="phone-screen">
            <div class="phone-notch"></div>
            <div class="status-bar">
                <div class="status-left">
                    <span>9:41</span>
                </div>
                <div class="status-right">
                    <span>📶</span>
                    <span>📶</span>
                    <span>🔋</span>
                </div>
            </div>
            <div class="content-wrapper">
                <img src="/header.png" alt="Header" class="header-image" />
                <div class="webview-container" id="webviewContainer">
                    <iframe 
                        id="webviewFrame" 
                        class="webview-iframe" 
                        src="<%= webviewUrl %>"
                        allow="geolocation; microphone; camera; payment; fullscreen">
                    </iframe>
                </div>
                <div class="away-message" id="awayMessage">
                    You went somewhere else
                </div>
                <div class="footer-image">
                    <img src="/footer.png" alt="Footer" />
                    <div class="footer-overlay">
                        <div class="footer-click-area left-area" id="leftFooterArea"></div>
                        <div class="footer-click-area right-area" id="rightFooterArea"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script>
        let isAway = false;
        const webviewContainer = document.getElementById('webviewContainer');
        const awayMessage = document.getElementById('awayMessage');
        const leftFooterArea = document.getElementById('leftFooterArea');
        const rightFooterArea = document.getElementById('rightFooterArea');

        leftFooterArea.addEventListener('click', function() {
            isAway = true;
            webviewContainer.classList.add('hidden');
            awayMessage.classList.add('visible');
        });

        rightFooterArea.addEventListener('click', function() {
            if (isAway) {
                isAway = false;
                webviewContainer.classList.remove('hidden');
                awayMessage.classList.remove('visible');
            }
        });
    </script>
</body>
</html>
